<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Margin="20">
    
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header Image -->
    <Image x:Name="ImageLabel" Height="100" Stretch="Uniform"/>

    <!-- Description Text -->
    <TextBlock Grid.Row="1" x:Name="descriptionLabel" Margin="0,10,0,10"
               FontSize="14" TextWrapping="Wrap" />

    <!-- EULA Heading -->
    <TextBlock FontWeight="Bold" FontSize="14" Margin="0,10,0,5" Text="End User License Agreement"/>

    <!-- EULA Content -->
    <RichTextBox Grid.Row="2" x:Name="ulaTextBox" 
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 Height="250"
                 BorderThickness="1"
                 BorderBrush="LightGray"
                 Margin="0,0,0,10"/>

    <!-- Consent Text -->
    <TextBlock Grid.Row="3" x:Name="consentLabel" Margin="0,5,0,5"
               TextWrapping="Wrap" FontSize="12" Foreground="Gray" />

    <!-- Buttons -->
    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
        <Button x:Name="CancelBtn" Width="100" Height="35" Margin="0,0,558,0"/>
        <Button x:Name="NextBtn" Width="100" Height="35"/>
    </StackPanel>
</Grid>

<Window x:Class="PDFVictory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PDFVictory" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Logo -->
            <RowDefinition Height="Auto"/>
            <!-- Description -->
            <RowDefinition Height="*"/>
            <!-- EULA -->
            <RowDefinition Height="Auto"/>
            <!-- Consent -->
            <RowDefinition Height="Auto"/>
            <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Header Image -->
        <Image x:Name="ImageLabel" Height="100" Stretch="Uniform"/>

        <!-- Description Text -->
        <TextBlock Grid.Row="1" x:Name="descriptionLabel" Margin="0,10,0,10"
                   FontSize="14" TextWrapping="Wrap" />

        <!-- EULA (RichTextBox) -->
        <TextBlock FontWeight="Bold" FontSize="14" Margin="0,10,0,5"/>

        <RichTextBox Grid.Row="2" x:Name="ulaTextBox" 
             IsReadOnly="True"
             VerticalScrollBarVisibility="Auto"
             Height="250"
             BorderThickness="1"
             BorderBrush="LightGray"
             Margin="0,0,0,10"/>


        <!-- Consent Text -->
        <TextBlock Grid.Row="3" x:Name="consentLabel" Margin="0,5,0,5"
                   TextWrapping="Wrap" FontSize="12" Foreground="Gray" />

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CancelBtn" Width="100" Height="35" Margin="0,0,558,0" Click="CancelBtn_Click"/>
            <Button x:Name="NextBtn" Width="100" Height="35" Click="NextBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>

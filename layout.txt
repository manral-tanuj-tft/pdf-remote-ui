<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Margin="20">

    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>  <!-- Image -->
        <RowDefinition Height="Auto"/>  <!-- Description -->
        <RowDefinition Height="Auto"/>  <!-- Heading -->
        <RowDefinition Height="*"/>     <!-- EULA -->
        <RowDefinition Height="Auto"/>  <!-- Consent -->
        <RowDefinition Height="Auto"/>  <!-- Buttons -->
    </Grid.RowDefinitions>

    <!-- Logo -->
    <Image x:Name="ImageLabel" Grid.Row="0" Height="80" Stretch="Uniform" Margin="0,0,0,10"/>

    <!-- Description -->
    <TextBlock Grid.Row="1" x:Name="descriptionLabel"
               FontSize="13" TextWrapping="Wrap"
               Margin="0,0,0,10" />

    <!-- Heading -->
    <TextBlock Grid.Row="2" FontWeight="Bold" FontSize="13"
               Text="End User License Agreement" Margin="0,0,0,5"/>

    <!-- EULA -->
    <RichTextBox Grid.Row="3" x:Name="ulaTextBox" 
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 Height="180"
                 BorderThickness="1"
                 BorderBrush="LightGray"
                 Margin="0,0,0,10"/>

    <!-- Consent -->
    <TextBlock Grid.Row="4" x:Name="consentLabel" Margin="0,5,0,5"
               TextWrapping="Wrap" FontSize="12" Foreground="Gray" />

    <!-- Buttons -->
    <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
        <Button x:Name="CancelBtn" Width="100" Height="35" Margin="0,0,10,0"/>
        <Button x:Name="NextBtn" Width="100" Height="35"/>
    </StackPanel>
</Grid>
